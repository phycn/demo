package cn.puhy.demo.geo.algorithm;

public class GeoService {

    private GeoGetCodeStrategy strategy;

    public GeoService(GeoGetCodeStrategy strategy) {
        this.strategy = strategy;
    }
}
